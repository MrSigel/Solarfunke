# Supabase – Lead-Persistenz (server-seitig). Werte in .env.local eintragen bzw.
# in den Deployment-Umgebungsvariablen (z. B. Vercel Project Settings) setzen.
SUPABASE_URL=https://<dein-projekt>.supabase.co
SUPABASE_ANON_KEY=<dein-anon-key>
